package domain

import org.specs2.mutable._
import org.specs2.specification._

import org.joda.time._


/*
class CampaignHistorySpec extends Specification with AllExpectations{

  "budget()" should {
    "select the latest (current) budget" in {
      val campHistory = createCampaignHistory
      campHistory.campaign.budget must_==(campHistory.budgetHistory.last.elem)
    }

  }

}
*/
